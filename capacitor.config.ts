// Configurazione per Capacitor
const config = {
  appId: 'com.replit.calcolatoreinteresserischio',
  appName: 'Calcolatore Interesse Rischio',
  webDir: 'dist',
  bundledWebRuntime: false,
  server: {
    androidScheme: 'https'
  }
};

export default config;